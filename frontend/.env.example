# URL da API embutida no bundle (Vite). Rebuild ao mudar (npm run build ou docker build --build-arg).
# Deve apontar para a API desta sessão; não hardcodar hostname AWS no código.
VITE_API_URL=http://localhost:8080
